extends layouts/main

block content
    .form-container
        form(action=`/users${routes.changePassword}`, method="post")
            input(type="password", id="oldPassword", name="Current Password")
            input(type="password", id="newPassword", name="New Password")
            input(type="password", id="newPassword1", name="Verify New Password")
            input(type="submit", value="Change Password")